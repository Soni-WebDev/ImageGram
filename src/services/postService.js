export const createPost = async (createPostObj)=>{
    // 1. take the img of the post and upload on aws
    // 2. get the urlof the img from the aws response
    // 3. create a post with the caption and the img url in the db using repositiry
    // 4. return the post obj
    
}